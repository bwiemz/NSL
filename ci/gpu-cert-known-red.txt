# GPU certification lane — known-red gates
# =========================================
#
# Gates listed here are ALLOWED to fail without failing the lane. Everything
# else that is not PASS makes `scripts/gpu-cert.sh --run` exit 1.
#
# This file started at 19 entries. Triage closed all but the two below:
#
#   - tier_b2 full backward x4   REAL BUG: the launcher passed 49 args to a
#                                50-param kernel (62b64e37). Fixed.
#   - csha_gap_gpu_e2e x1        BAD TEST: an all-ones probe makes dS vanish
#                                identically, so dWq/dWk are zero as a matter
#                                of arithmetic. Kernel was correct.
#   - ZeRO collectives x2        STALE: counts encoded the pre-f1030f76 world
#                                where `model.to(device)` skipped fields after
#                                an inline model array.
#   - zero_reduce_grads x1       STALE: asserted success where the codebase now
#                                deliberately REFUSES (-5) a device-resident
#                                gradient on a non-CUDA-aware backend.
#   - cuda_graph x1              UNDER-PROVISIONED: capture needs a warm-up, and
#                                the fixture ended on the capturing occurrence,
#                                so `replays` could never be > 0.
#   - cycle-15 ablations x3      Reclassified `diagnostic`: they are Bug-1
#                                bisection probes whose pass/fail pattern IS
#                                the output.
#   - checkpoint_recompute x4    Reclassified `broken`: R7 refuses rope_q +
#                                @checkpoint, so synthesis never runs. The
#                                refusal is now pinned by
#                                csha_r7_checkpoint_rope_q_refusal.rs.
#
# Deleting a line is how a fix gets locked in — the lane enforces that gate
# forever after.
#
# Format (tab-separated, matching the report's first two columns):
#   <cargo target args><TAB><test name>

# ── Structurally unreachable under the lane (6) ──────────────────────────
# The first two are #[cfg(not(feature = "cuda"))] — they exist ONLY in
# non-CUDA builds, so a CUDA sweep can never list them. Not breakage; recorded
# so the NOTFOUND is accounted for rather than silently ignored.
#
# The four nccl_cert gates are the mirror image: #[cfg(feature = "nccl")], and
# CERT_FEATURES (gpu-cert.sh:110) is cuda,test-hooks,test-helpers,nsl-test/cuda
# with no `nccl`, so this lane compiles them out. Adding `nccl` to the lane is
# NOT the fix — cudarc dlopens libnccl.so.2 at runtime and no libnccl is
# system-installed on the cert host, so the lane would go from NOTFOUND to a
# load failure.
#
# These are certified by `scripts/nccl-cert.sh` instead, which builds with
# --features nccl against an explicit NSL_NCCL_LIB_DIR and prints a verdict
# naming what it could and could not prove. If this lane ever gains a real
# libnccl, DELETE these four lines — that is how the coverage gets locked in.
-p nsl-runtime --test fp8_dispatcher 	cuda_feature_required
-p nsl-cli --test wrga_gatedlora_backward_trigger 	wrga_b32_trigger_measurement_requires_cuda
-p nsl-runtime --lib 	a_real_one_rank_communicator_initializes_and_destroys
-p nsl-runtime --lib 	nccl_device_collectives_actually_execute_on_this_gpu
-p nsl-runtime --lib 	unmapped_dtypes_are_refused_before_reaching_nccl
-p nsl-runtime --lib 	the_trait_methods_short_circuit_at_ws1_without_entering_nccl

# ── Pre-existing, filed (1) ────────────────────────────────────────────
# arena_parity_script_passes: the cuda-graph arm is VACUOUS — both arms end
# captured=0 taints=2 (bf16-storage casts + batched-sgemm taint capture keep
# the regions eager, so the shipped perf default can never capture; filed
# during the #522-era session). Measured on this branch 2026-08-24: the
# loss-stream parity half PASSES over the full arms; only the vacuity check
# fails, ~8 min under a release nsl. The lane runs the DEBUG binary, which
# may report TIMEOUT rather than FAIL — both are excused by this row.
# Delete this line when graph capture works again — the lane then enforces
# the gate forever after.
-p nsl-cli --test transient_arena_gpu_gate 	arena_parity_script_passes
