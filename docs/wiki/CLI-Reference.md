# CLI reference

<!-- GENERATED by `nsl doc cli` from crates/nsl-cli/src/args.rs. Do not edit by hand: `cargo test -p nsl-cli` fails if this page and the command tree differ. -->

Every `nsl` subcommand (24 of them), with each one's positionals and flags in the words of `nsl <command> --help` (this page and the help text are rendered from the same declarations). Flags are listed with the value they take (a flag with no value is a switch; `(optional)` means the flag may be given bare), the default that applies when the flag is absent, and — for an enumerated flag — the accepted values. `nsl --version` prints the toolchain version; `nsl <command> --help` prints one command's page.

## Commands

| Command | Purpose |
|---|---|
| [`nsl check`](#nsl-check) | Parse and type-check an NSL file |
| [`nsl run`](#nsl-run) | Compile and execute an NSL program |
| [`nsl build`](#nsl-build) | Compile an NSL file to a native executable |
| [`nsl test`](#nsl-test) | Run @test functions in an NSL file |
| [`nsl export`](#nsl-export) | Export model to ONNX or checkpoint to safetensors |
| [`nsl convert`](#nsl-convert) | Convert between checkpoint formats (.nslm ↔ .safetensors) |
| [`nsl init`](#nsl-init) | Initialize a new NeuralScript project |
| [`nsl fmt`](#nsl-fmt) | Format NeuralScript source files |
| [`nsl debug`](#nsl-debug) | Read and analyze a tensor operation trace (.nsltrace binary) |
| [`nsl zk`](#nsl-zk) | M55: ZK inference circuit operations (stats, prove, verify) |
| [`nsl zk stats`](#nsl-zk-stats) | Show circuit statistics for a compiled .zkir file |
| [`nsl zk prove`](#nsl-zk-prove) | Generate a ZK proof from a compiled circuit |
| [`nsl zk verify`](#nsl-zk-verify) | Verify a ZK proof against public inputs |
| [`nsl profile`](#nsl-profile) | Predictive performance profile for a target GPU |
| [`nsl autotune`](#nsl-autotune) | Measure @autotune kernel variants on this GPU and cache the winners (roadmap item 10 — the measured path; compiles themselves never benchmark). A Measured cache record short-circuits the cost model at every subsequent compile of the same kernel on the same device |
| [`nsl tokenize`](#nsl-tokenize) | Train a BPE tokenizer from source files |
| [`nsl fpga-compile`](#nsl-fpga-compile) | M57: Compile an NSL file to synthesizable Verilog for FPGA targets |
| [`nsl ptx-metadata`](#nsl-ptx-metadata) | Report static per-kernel PTX metadata (declared registers, shared memory, target SM) parsed from a synthesized `.ptx` file. Pure text analysis — no GPU or CUDA toolkit required |
| [`nsl env`](#nsl-env) | The `NSL_*` environment variables the toolchain reads: list the registry, or show which are set in the current environment |
| [`nsl env list`](#nsl-env-list) | Print the registry: every variable with its kind, default, tier and doc |
| [`nsl env current`](#nsl-env-current) | Show the NSL_* variables set in this environment and their values, registered ones first; an NSL_* variable that is set but not in the registry is flagged (and makes the exit code 1 with --strict) |
| [`nsl doc`](#nsl-doc) | Generated reference documentation: the CLI and standard-library reference pages |
| [`nsl doc cli`](#nsl-doc-cli) | Emit the Markdown CLI reference that docs/wiki/CLI-Reference.md is generated from: every subcommand with its positionals and flags, in the words of `nsl <command> --help` |
| [`nsl doc stdlib`](#nsl-doc-stdlib) | Emit the Markdown standard-library reference that docs/wiki/Stdlib-Reference.md is generated from: every module in `stdlib/` with its functions and models, signatures from the parser and descriptions from `##` doc comments |

## `nsl`

NeuralScript Language Toolchain

```text
nsl <COMMAND>
```

## `nsl check`

Parse and type-check an NSL file

```text
nsl check [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--dump-tokens` |  |  | Print the token stream |
| `--dump-ast` |  |  | Print the AST as JSON |
| `--dump-types` |  |  | Print the inferred type map |
| `--shapes` |  |  | Print a compile-time shape-propagation trace |
| `--perf` |  |  | M37: Run roofline performance analysis (delegates to the `nsl profile` engine with its defaults; use `nsl profile` directly for entry-point/dim/JSON control) |
| `--gpu` | `<GPU>` |  | M37: Target GPU for performance analysis (e.g., "H100", "A100-PCIe"); requires --perf |
| `--trace` | `<TRACE>` |  | M37: Write Chrome tracing JSON to file. NOT IMPLEMENTED on `nsl check` — refused with an error; use `nsl debug <file.nsltrace> --export-chrome <out>` instead |
| `--linear-types` |  |  | M38a: Enable linear types ownership checking |
| `--nan-analysis` |  |  | M45: Run compile-time NaN/Inf risk analysis |
| `--deterministic` |  |  | M46: Deterministic mode. Compile-time non-determinism detection AND runtime routing to bit-reproducible kernel variants: the no-atomics embedding backward, the non-coalesced `sum_dim` route, and the flash-attention backward (whose phase-2 kernel accumulates dQ with a cross-CTA float atomicAdd, so its rounding follows scheduling order). The flash backward has no deterministic GPU variant yet, so this routes it to the CPU reference — correct, and roughly 36x the per-step cost at Coder-50M. `--deterministic` already makes that trade elsewhere (its deterministic sum/mean kernels are sequential single-thread PTX); budget for it before using this on a long run. |
| `--weight-analysis` |  |  | M52: Run weight analysis report |
| `--weights` | `<WEIGHTS>` |  | M52: Path to safetensors weight file for weight analysis |
| `--dead-weight-threshold` | `<DEAD_WEIGHT_THRESHOLD>` | `0.000001` | M52: Dead weight threshold for analysis (default: 1e-6) |
| `--sparse-threshold` | `<SPARSE_THRESHOLD>` | `0.5` | M52: Sparsity threshold for analysis (default: 0.5) |
| `--training-report` | `<TRAINING_REPORT>` (optional; give a value as `=VALUE`) |  | Emit a training-pipeline decision audit for every train block in the file. Pass without value for text output, or `--training-report=json` for JSON [possible values: `text`, `json`] |
| `--cep-search` |  |  | CEP: run hardware-aware architecture search |
| `--cep-profile` |  |  | CEP: print a bare CompilationProfile for the model (paper §7.1 differentiator — equivalent of PyTorch's missing 'nsl check --perf') |
| `--cep-target` | `<CEP_TARGET>` |  | CEP: target GPU for analysis (e.g. H100-SXM) |
| `--cep-out` | `<CEP_OUT>` |  | CEP: write the delta JSON here (default: &lt;model&gt;.cep.json) |
| `--cpkd-design-student` | `<PARAMS>` |  | CPKD Innovation 5: design a student architecture under a parameter budget via the CEP search oracle and print the teacher-&gt;student layer mapping. Budget accepts a raw integer or K/M/B suffix (e.g. "125M", "1.1B", "350000000"). Requires @search axes on the model. Uses `--weights` (if given) for teacher layer importance; without weights the mapping degenerates to the first k layers |
| `--cpkd-target` | `<GPU>` |  | CPKD: target GPU for student design (e.g. "H100-SXM", "RTX-4090"); defaults to H100-SXM. Requires --cpkd-design-student |
| `--wrga-analyze` | `<WRGA_ANALYZE>` (optional; give a value as `=VALUE`) |  | WRGA paper §8.3: emit the WRGA compilation report without running codegen output. Pass without value (or `-`) for stdout; provide a path to write to a file. Sister of `nsl build --wrga-report` but avoids producing a `.o`. Requires `@wrga` decorators in the source |
| `--wrga-target` | `<WRGA_TARGET>` |  | WRGA paper §8.3: target GPU for the WRGA roofline analysis (e.g. "H100-SXM", "A100-PCIe", "RTX-4090"). Overrides any `target=` set on `@wrga(...)` decorators. Looked up in the codegen GPU spec database; empty / unknown falls back to the database default |
| `--wrga-compare` | `<WRGA_COMPARE>` (optional; give a value as `=VALUE`) |  | WRGA paper §8.3: emit a structural PEFT comparison report (WRGA vs. LoRA / AdaLoRA / GaLore / ReFT) without running codegen output. Pass without value (or `-`) for stdout; provide a path to write to a file. Mutually exclusive with `--wrga-analyze`. Requires `@wrga` decorators in the source |
| `--csha` | `<MODE>` (optional) |  | CSHA: attention-fusion mode ("auto", "boundary", "pipeline", "block", or "off").  Passing `--csha` without a value enables auto mode.  Mirrors `nsl run --csha` / `nsl build --csha`; drives the planner-only diagnostic surfaced by `--csha-report` |
| `--csha-report` |  |  | CSHA: print the attention-fusion report (paper §6.3) to stderr. Mirrors `nsl run --csha-report` / `nsl build --csha-report`. Runs the planner against the file's train block (if any) and emits the per-layer Boundary / Pipeline / Block tier decisions plus the backward-tier choice. No kernels are emitted |
| `--wrga-ablate` | `<WRGA_ABLATE>` |  | WRGA paper §9.3: ablate one or more Innovations from this run. Comma-separated tokens from `wengert \| roofline \| spectral \| fusion \| memory \| all \| none`. Default `none` (no ablation). `all` is shorthand for every Innovation. Combines with `--wrga-analyze` / `--wrga-compare` so users can measure the per-Innovation contribution to the report. Examples: nsl check --wrga-analyze --wrga-ablate=fusion model.nsl nsl check --wrga-compare --wrga-ablate=wengert,fusion model.nsl |
| `--activation-report` |  |  | Milestone A: print the activation-contract report — one line per requested optimization surface (flag or decorator) with what became of it (applied / declined / witnessed / UNSATISFIED). On `nsl check` the report is informational only; enforcement happens on `build`/`run`, where the compile actually runs |
| `--allow-inert-requests` |  |  | Milestone A escape hatch: demote unsatisfied activation contracts (a requested feature whose owner recorded no disposition) from a hard error to a warning. For migration only — an inert request is a correctness bug in the invocation or the compiler |
| `--allow-unknown-decorators` |  |  | Milestone A escape hatch: demote UNKNOWN-decorator-name errors to warnings (one release, for migration). Documented-but-unimplemented decorators (e.g. @tie_weights) stay hard errors — accepting those would resurrect the silence this flag's namespace close exists to end |

## `nsl run`

Compile and execute an NSL program

```text
nsl run [OPTIONS] <FILE> [-- <ARGS>...]
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file |
| `<ARGS>` | no |  | Arguments to pass to the compiled program |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--profile-memory` |  |  | Enable memory profiling (writes memory_profile.json on exit) |
| `--profile-kernels` |  |  | Enable kernel profiling (writes kernel_profile.json on exit) |
| `--profile` |  |  | Enable all profilers and merge into profile.json |
| `--devices` | `<DEVICES>` | `1` | Number of GPUs for tensor parallelism (spawns N processes) |
| `--collectives` | `<COLLECTIVES>` | `sim` | P4 item 14: collective backend for multi-rank runs — "sim" (CPU-shm reference, default), "sim-gpu" (CUDA-aware TEST backend — device-pointer API staged through the CPU-shm reduce; validates the GPU plumbing on one GPU), or "nccl" (real CUDA-aware collectives; requires an nccl-featured build with libnccl) |
| `--prefill-workers` | `<PREFILL_WORKERS>` | `1` | M41: Number of prefill workers for disaggregated inference |
| `--decode-workers` | `<DECODE_WORKERS>` | `1` | M41: Number of decode workers for disaggregated inference |
| `--target` | `<TARGET>` | `cuda` | M47: GPU target backend (cuda, rocm, metal, webgpu) |
| `--disable-fusion` |  |  | Disable all fusion optimizations (for differential testing) |
| `--tape-ad` |  |  | Force tape-based AD (disable source-to-source AD) |
| `--source-ad` |  |  | Use compile-time source-to-source AD instead of runtime tape AD |
| `--pretrain-optimized` |  |  | Pretraining bundle: expands to `--source-ad --wggo greedy --csha auto --fuse-rmsnorm-backward --fuse-wgrad-accum` so one invocation runs WGGO planning, applies CSHA + FASE per the plan, and takes both backward fusions. Explicit flags win (only unset values are filled); conflicts with `--tape-ad` (the planning stack lives in the source-AD path). The two fusions change the ARITHMETIC, not just the schedule — that is why they are opt-in elsewhere. Measured on Coder-50M (batch 4, seq 1024): +14% tok/s, max relative loss divergence 1.2e-5 over 24 steps with an identical trend — a number produced by `--fuse-rmsnorm-backward` and the rest of the bundle, NOT by `--fuse-wgrad-accum`, which needs `grad_accumulation >= 2` in the train block and fused nothing in that run (codegen says so per train block: `[wgrad-fusion] declined: ...`). `--training-reference` strips them back out for a reference run, and `--fuse-wgrad-accum` is skipped (with a note) when `--grad-integrity`, `--optim-state-offload` or `--layerwise-accum` is set, since it refuses those. |
| `--debug-training` |  |  | Debug training: disable fusion + FBIP, emit gradient checksums |
| `--grad-integrity` |  |  | P0.3: gradient-integrity gate — assert every trainable parameter gets a finite, mostly-nonzero gradient each step; prints the [grad-integrity] snapshot at exit. Observes only (no opt changes) |
| `--training-reference` |  |  | P1.7: reference-training mode — disable every training optimization (CCR, CSLA, weight-stream, offload, WGGO, CPDT/precision, CSHA, @fuse, FBIP, fused FASE step, fused-CE) for an independent baseline |
| `--trace-ops` |  |  | M45: Enable tensor operation tracing (writes .nsl.trace binary) |
| `--deterministic` |  |  | M46: Deterministic mode. Compile-time non-determinism detection AND runtime routing to bit-reproducible kernel variants: the no-atomics embedding backward, the non-coalesced `sum_dim` route, and the flash-attention backward (whose phase-2 kernel accumulates dQ with a cross-CTA float atomicAdd, so its rounding follows scheduling order). The flash backward has no deterministic GPU variant yet, so this routes it to the CPU reference — correct, and roughly 36x the per-step cost at Coder-50M. `--deterministic` already makes that trade elsewhere (its deterministic sum/mean kernels are sequential single-thread PTX); budget for it before using this on a long run. |
| `--matmul-mode` | `<MODE>` | `tf32` | Matmul arithmetic for high-intensity GEMMs: tf32 (default), bf16, f32. Replaces NSL_MATMUL_BF16. Unlike the environment variable this reaches the EXECUTION FINGERPRINT, so a checkpoint records which arithmetic produced it and a resume refuses a silent switch. The fingerprint's `dtype` key does NOT carry this: it is the model dtype and reads `bf16` whatever the GEMMs actually do. |
| `--bf16-rounding` | `<MODE>` | `rne` | Rounding for the bf16 operand cast: rne (default) or sr. Replaces NSL_MATMUL_BF16_ROUND. SR re-dithers per launch, which blocks CUDA graph capture and is incompatible with --bf16-cast-cache |
| `--bf16-min-ratio` | `<RATIO>` | `512` | Minimum arithmetic intensity mnk/(a+b elements) for a GEMM to take the bf16 path. Replaces NSL_MATMUL_BF16_MIN_RATIO. ARITHMETIC: it decides WHICH matmuls are reduced precision |
| `--bf16-cast-cache` |  |  | Cache the weight operand's bf16 cast across GEMMs. Replaces NSL_MATMUL_BF16_CAST_CACHE. Bit-preserving, so a resume only warns -- but it costs ~2 GiB of pinned VRAM at 1B, so enabling it on a resume can OOM a run that previously fit |
| `--bf16-lt` |  |  | Issue bf16-storage GEMMs through cuBLASLt heuristics rather than GemmEx. Replaces NSL_MATMUL_BF16_LT. Changes kernel and reduction order, so it is arithmetic-class for resume |
| `--bf16-lt-workspace-mib` | `<MIB>` | `64` | Workspace cap in MiB for the cuBLASLt heuristic (clamped to 4096). Replaces NSL_MATMUL_BF16_LT_WORKSPACE_MIB. ARITHMETIC: the cap FILTERS candidate kernels, so a smaller value excludes split-k and wide-tile algorithms and changes the reduction order |
| `--no-bf16-lt-tune` |  |  | Disable cuBLASLt timed first-use plan selection. Replaces NSL_MATMUL_BF16_LT_TUNE=0. With tuning ON (the default) the winner depends on live machine state, so plan choice is NOT reproducible across processes |
| `--seed` | `<SEED>` |  | P0 certification: RNG seed for randn/rand/stochastic ops. Overrides the historical 42 under --deterministic and also seeds non-deterministic runs (distinct reproducible inits for multi-seed campaigns) |
| `--distribute` | `<DISTRIBUTE>` |  | M43: 3D parallelism config (e.g., "dp=2, tp=4, pp=4") |
| `--zero-stage` | `<ZERO_STAGE>` |  | M43: ZeRO optimizer sharding stage (1, 2, or 3) |
| `--zero-elementwise` |  |  | Item 11: elementwise 1/ws parameter sharding under --zero-stage 3 (see `nsl build --help` for the full description) |
| `--wcet` |  |  | M53: Enable WCET analysis for @real_time functions |
| `--wcet-cert` | `<WCET_CERT>` |  | M53: Write WCET certificate JSON to file |
| `--gpu` | `<GPU>` |  | M53: GPU target for WCET analysis (e.g., "A100-SXM", "Orin") |
| `--cpu` | `<CPU>` |  | M53: CPU target for WCET analysis (e.g., "cortex-a78") |
| `--do178c-report` | `<DO178C_REPORT>` |  | M53: Write DO-178C compliance report to file (FPGA only) |
| `--wcet-target` | `<WCET_TARGET>` | `gpu` | M53: WCET target: "gpu" (statistical advisory), "fpga" (certified DO-178C), "groq" (blocked) |
| `--fpga-device` | `<FPGA_DEVICE>` |  | M53: FPGA device for certified WCET (e.g., "xcvu440", "xczu9eg", "ve2302") |
| `--cuda-sync` |  |  | Synchronize after every CUDA kernel launch (debug: surfaces async GPU errors) |
| `--gpu-mem-report` |  |  | ELTLS instrumentation: print a GPU memory report at end of run. Prints epilog_frees_total plus (with --features cuda) the live caching-allocator block summary and driver/allocator stats |
| `--monitor` |  |  | Render predicted-vs-actual kernel timings instead of running the program |
| `--health-interval` | `<N>` |  | Health-monitor flush interval in steps (default 100; requires --monitor) |
| `--inspect` |  |  | Activate @inspect hooks: dump tensor stats/contents to .nsl-inspect/ |
| `--csha` | `<MODE>` (optional) |  | CSHA: attention-fusion mode ("auto", "boundary", "pipeline", "block", or "off").  Passing `--csha` without a value enables auto mode.  Mirrors `nsl build --csha` |
| `--csha-report` |  |  | CSHA: print the attention-fusion report to stderr |
| `--linear-types` |  |  | M38a/M56: Enable linear types ownership checking. Required for agent declarations (M56). Closes Task 20 of the M56 plan |
| `--cpdt` | `<MODE>` (optional) |  | CPDT: planner mode ("full", "zero_only", or "off"). Passing `--cpdt` without a value enables full mode. Mirrors `nsl build --cpdt` so precision-adaptive training executes end-to-end via `nsl run` |
| `--cpdt-num-gpus` | `<N>` |  | CPDT: number of GPUs in the target cluster. Required when `--cpdt` is set |
| `--cpdt-intra-bw` | `<BPS>` | `900000000000` | CPDT: intra-node bandwidth in bytes/sec (default 9e11 = 900 GB/s) |
| `--cpdt-inter-bw` | `<BPS>` | `100000000000` | CPDT: inter-node bandwidth in bytes/sec (default 1e11 = 100 GB/s) |
| `--cpdt-report` |  |  | CPDT: emit the full plan to stdout. Implies `--cpdt` (full mode) |
| `--wggo` | `<MODE>` (optional) |  | WGGO: global-optimization mode ("full", "greedy", "off", or "auto"). Passing `--wggo` without a value enables full mode. Mirrors `nsl build --wggo` so the WGGO mode-table dispatch reaches `emit_unified_optim_step_dispatch` end-to-end via `nsl run` |
| `--wggo-report` |  |  | WGGO: print the global-optimization report to stderr |
| `--wggo-moment-precision` |  |  | WGGO: lower the plan's optimizer-moment precision decisions (optim_m/v_bits) to real m/v storage dtypes. Opt-in: changes training numerics. Works on CPU and GPU (the GPU cast envelope uses the CFTP-v7 PTX cast kernels) |
| `--wggo-weights` | `<PATH>` |  | WGGO Stage 3: path to a `.nslweights` sidecar for real weight-based importance scoring. Without this flag the analyzer falls back to uniform head scores |
| `--wggo-importance` | `<WGGO_IMPORTANCE>` | `auto` | WGGO head-importance scoring mode. - `auto` (default): gradient scoring when calibration sidecar present, else magnitude. - `magnitude`: force magnitude scoring even with calibration data. - `grad`: require gradient scoring; errors if no calibration sidecar present [possible values: `auto`, `magnitude`, `grad`] |
| `--wggo-prune-fraction` | `<F>` |  | WGGO Stage 3: fraction of heads the default `min_retained_importance` threshold allows to be pruned. Clamped to [0.0, 0.9]; default 0.25 |
| `--wggo-memory-budget` | `<MIB>` |  | WGGO: per-device resident training-memory budget in MiB. When set, the planner enforces the cap at both optimization levels (inter-layer DP + per-layer ILP) and, under budget pressure, organically lowers Adam-moment precision to fit — implies --wggo-moment-precision (fp16 moments via the #369 GPU cast envelope are the shipped mechanism). Rejected if 0 or unparseable. A plan that cannot fit even with 16-bit moments is a hard compile error |
| `--optim-state-offload` |  |  | Optimizer-state offload (single-GPU ZeRO-Offload analog): keep the Adam/momentum state (m/v) HOST-resident (pinned when a GPU is live) and stage it to the device for each optimizer step, freeing 1-2x parameter bytes of VRAM. The update math still runs on the GPU in f32, so FASE == AdamW exactness is preserved; the copy-back overlaps the next parameter's update on a dedicated transfer stream. COMPOSES with reduced-precision moments (--wggo-moment-precision / --wggo-memory-budget): host state is then stored at the planned dtype and staged through an on-device quant/dequant envelope, halving the PCIe staging traffic. Env kill-switches: NSL_OFFLOAD_SYNC=1 (synchronous copy-back), NSL_OFFLOAD_PAGEABLE=1 (pageable host buffers). Not supported on pipelined train blocks |
| `--checkpoint-blocks` |  |  | CCR (docs/research/CCR.pdf) block-granular activation checkpointing on the source-AD path: each transformer block's interior activations are freed after the forward and recomputed just before that block's backward, so activation residency drops from O(layers x interiors) to O(layers x boundaries + one block's interiors). Bit-exact — the recompute replays the same kernels in the same order on the same inputs (dropout results are never replayed; they stay saved). No-op with a stderr note on models without `blocks.N`-style structure |
| `--checkpoint-selective` |  |  | With --checkpoint-blocks: selective policy (never recompute matmul-class ops; replay only cheap norms/RoPE/elementwise). Less memory reduction at near-zero recompute cost |
| `--checkpoint-budget-mib` | `<CHECKPOINT_BUDGET_MIB>` |  | With --checkpoint-blocks: allowed SAVED-interior activation budget in MiB. The CCR knapsack flips the most expensive-to-recompute tensors back to SAVE within the budget. Under FASE Deferred the freed-gradient-buffer credit (C-01) is added automatically when parameter sizes are statically known |
| `--checkpoint-stride` | `<CHECKPOINT_STRIDE>` | `1` | With --checkpoint-blocks: periodic checkpointing (Item 8). `N` (an integer &gt;= 1) coalesces every N transformer-block anchors into one CCR super-segment — saving only every N-th block boundary and recomputing the N-block span, for a ~N x smaller saved-boundary surface (the activation surface CSLA buffers across the accumulation window). `auto` searches strides against --checkpoint-budget-mib and picks the smallest projected activation peak that fits. `1` (default) = classic per-block. Bit-exact at any stride. `N` coalesces every N block anchors; `auto` searches uniform strides against the projected activation peak; `dp` (P5 item 21) runs the non-uniform partition DP — minimum GpuSpec-calibrated recompute time subject to --checkpoint-budget-mib, with the CSLA window applied, fusion-aware launch costs and a conservative prefetch-overlap credit. Any choice is bit-exact |
| `--fuse-rmsnorm-backward` |  |  | Item 9 + P5 item 20: lower the source-AD RMSNorm input gradient AND the gamma gradient to fused kernels (native GPU / CPU) instead of the ~11-op / 7-op decompositions — fewer launches, no [rows, cols] temporaries, less HBM traffic. Requires source-AD (`--source-ad`, or `--pretrain-optimized`, which enables this too). Matches the decomposition to an f32 tolerance (opt-in speedup); the fused paths are bit-deterministic run-to-run |
| `--fuse-wgrad-accum` |  |  | Item 7: collapse the source-AD weight-gradient chain (transpose -&gt; batched matmul -&gt; reduce) AND the FASE Deferred accumulate into ONE cuBLAS call, writing straight into m_partial. Removes the [B, d, o] raw-gradient temporary — B times the size of the parameter — and the reduce's full read-back over it. Requires source-AD (`--source-ad`, or `--pretrain-optimized`, which enables this too) and a FASE-Deferred plan. A Deferred plan needs `grad_accumulation >= 2` in the train block. Every block that cannot reach it is named on stderr (`[wgrad-fusion] declined: train block #N — ...`); the flag is REFUSED only when no block in the whole program could fuse, so a program that accumulates in one block and not another still builds. NOT bit-exact: products are summed in cuBLAS's order rather than rounding each per-batch partial first. Measured against an f64 reference it is closer to the true gradient than the chain it replaces in 2 of 3 shapes, but validate loss parity at the repo's 3-4 dp standard before relying on it. Refuses --grad-integrity (which must read the raw gradient this never materializes), --optim-state-offload (host-resident m_partial the device GEMM cannot write), and --layerwise-accum (pre-sliced tapes defeat the fusion's single-reader proof). |
| `--checkpoint-compress` | `<CHECKPOINT_COMPRESS>` |  | With --checkpoint-selective: compress the saved matmul-class activations to half precision between forward and backward (fp16 or bf16). Not bit-exact — backward reads rounded activations; validate loss parity at the 3-4 dp standard [possible values: `fp16`, `bf16`] |
| `--layerwise-accum` |  |  | CSLA Stage-2 (docs/research/CSLA paper §7): window-buffered training schedule. The N micro-batches of a FASE-Deferred accumulation window run their forwards first (buffering only the adjoint-read batch-dependent tensors), then the backward replays the whole window. Bit-exact with the interleaved baseline. Requires --checkpoint-blocks + --source-ad and a FASE-Deferred plan (AdamW/Adam, grad_accumulation &gt;= 2); refuses grad_clip / --optim-state-offload / mode tables loudly |
| `--weight-stream` |  |  | With --layerwise-accum: layer WEIGHT streaming (D2b). Layer- grouped params live in pinned host mirrors and hold a device buffer ONLY inside their brackets: the forward uploads each layer right before its CCR segment and evicts it after its last primal read; the window backward re-uploads per replay range and writes back after that layer's update. Teardown restores residency for model_save/eval. Byte-preserving / bit-exact |
| `--param-dtype` | `<DTYPE>` | `f32` | P4 item 17: authoritative parameter storage dtype for training. `bf16-sr` stores every streamed parameter as a device-resident BF16 buffer (no FP32 master copy) and applies the fused AdamW update with compiler-owned counter-based stochastic rounding (deterministic in --seed, the step, and the parameter/element index). Requires --weight-stream + the fused AdamW step; refuses Muon, ZeRO, offload, and reduced-precision-moment compositions [possible values: `f32`, `bf16-sr`] |
| `--transient-arena` |  |  | Item 5 (Milestone C p2 Stage-2B): place admitted backward temporaries at fixed transient-arena offsets instead of letting the caching allocator choose an address per step. Admission is narrow on purpose — statically sized, non-escaping, non-aliasing backward temporaries produced by an op proven to make exactly one device allocation. Saved tensors, views, external workspaces and optimizer-visible values are excluded by construction. `[arena] placement:` reports what was admitted and, more usefully, what was refused and to which rule. The payoff is address stability for CUDA-graph capture, not bytes. EXPERIMENTAL until `scripts/arena-parity.sh` is green: a liveness error here is silent memory corruption, which is why placement is staged behind the analysis rather than shipped with it. |
| `--fuse-lm-head` | `<MODE>` |  | Item 4: authorize the compiler to install a fused LM head that no `@fused_lm_ce` decorator asked for. `auto` recognises the `matmul(x, transpose(W)) [+ bias] -> cross_entropy` chain, reads `(vocab, hidden)` off the weight's declared dims and the row count off the DataLoader, and falls back to the composite path (saying why, on stderr) when any of that cannot be proven. `require` makes the fallback a compile error. `--pretrain-optimized` selects `auto`. An explicit `@fused_lm_ce` on a train block always wins, including `enabled = false`, which is an opt-out this must not overturn. [possible values: `off`, `auto`, `require`] |
| `--muon-state-dtype` | `<DTYPE>` | `f32` | P4 item 18: Muon optimizer-state storage ladder. `bf16` stores the first-moment (momentum) buffers in BF16 with an FP32 working buffer per update and a counter-based stochastically-rounded store (v stays f32). `int8-blockwise` and `int4-structural` are later ladder rungs — currently refused [possible values: `f32`, `bf16`, `int8-blockwise`, `int4-structural`] |
| `--muon-batch-ns` |  |  | Muon perf campaign: shape-grouped batched Newton-Schulz for all Muon-routed rank-2 hidden matrices — one batched-GEMM pipeline per optimizer step (tensor-core TF32 strided-batched GEMMs over persistent workspaces) instead of ~15 GEMM launches per matrix. GPU-only; tolerance-equivalent to the sequential primitive (the AdamW-routed arm stays bit-for-bit stdlib). Refuses --layerwise-accum, --optim-state-offload and --zero-stage |
| `--muon-resident-momentum` |  |  | Muon perf campaign: with --optim-state-offload, keep Muon-routed params' momentum DEVICE-resident (skips its per-step PCIe stage-in/writeback; AdamW state for embeddings/head/vectors stays offloaded). Removes the mixed recipe's only per-step optimizer-state round trip at the cost of one f32 momentum surface in VRAM |
| `--cuda-graphs` |  |  | P5 item 19: opportunistic per-region CUDA graph capture/replay. Each source-AD lowering (forward slice, backward layer range, recompute segment) records its launch sequence, captures it as a CUDA graph once stable across steps, and replays it with per-launch verification + eager self-repair on any divergence. Requires --source-ad; refuses ZeRO, @pipeline, --cuda-sync and the kernel profiler. Bit-exact (same kernels, same order, same stream). NSL_CUDA_GRAPHS=0 disables at runtime; NSL_CUDA_GRAPH_LOG=1 traces capture decisions |
| `--stream-arena` |  |  | Item 10 (requires --weight-stream): batch each layer's per-param transfers into ONE contiguous host&lt;-&gt;device transfer through a stable, reused device staging arena. Bit-exact with per-param |
| `--stream-prefetch` |  |  | Item 11 (requires --stream-arena): double-buffer the backward weight stream with async prefetch + per-pack CUDA events. Bit-exact |
| `--stream-async-writeback` |  |  | Item 11 writeback half (requires --stream-arena): async pack writeback DtoH on the transfer stream; scatter at drain points |
| `-w`, `--weights` | `<WEIGHTS>` |  | Path to the model weights file (.safetensors) for the weight-aware CPDT path. Mirrors `nsl build -w/--weights` |
| `--activation-report` |  |  | Milestone A: print the activation-contract report — one line per requested optimization surface (flag or decorator) with what became of it (applied / declined / witnessed / UNSATISFIED) |
| `--allow-inert-requests` |  |  | Milestone A escape hatch: demote unsatisfied activation contracts (a requested feature whose owner recorded no disposition) from a hard error to a warning. For migration only — an inert request is a correctness bug in the invocation or the compiler |
| `--allow-unknown-decorators` |  |  | Milestone A escape hatch: demote UNKNOWN-decorator-name errors to warnings (one release, for migration). Documented-but-unimplemented decorators (e.g. @tie_weights) stay hard errors — accepting those would resurrect the silence this flag's namespace close exists to end |

## `nsl build`

Compile an NSL file to a native executable

```text
nsl build [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `-o`, `--output` | `<OUTPUT>` |  | Output file path (default: input stem + .exe on Windows) |
| `--emit-obj` |  |  | Emit only the object file (skip linking) |
| `--dump-ir` |  |  | Print the Cranelift IR for each function |
| `--standalone` |  |  | Produce a zero-dependency standalone bundle (requires -w/--weights) |
| `-w`, `--weights` | `<WEIGHTS>` |  | Path to the model weights file to bundle with the standalone executable |
| `--embed-weights` | `<EMBED_WEIGHTS>` | `auto` | Weight embedding strategy: auto, always, never (default: auto) |
| `--embed-threshold` | `<EMBED_THRESHOLD>` | `268435456` | Size threshold in bytes above which auto mode streams weights instead of embedding (default: 256 MiB) |
| `--no-autotune` |  |  | Skip @autotune benchmarking; use middle values from each parameter range |
| `--autotune-fresh` |  |  | Re-run all @autotune benchmarks, ignoring cached results |
| `--autotune-db` | `<AUTOTUNE_DB>` |  | Frozen tuning database (written by `nsl autotune --freeze`). Records are validated per lookup — device identity, cache key, schema — and win over the per-machine cache when they match |
| `--autotune-db-sha256` | `<AUTOTUNE_DB_SHA256>` |  | Refuse the tuning database unless its content sha256 matches — the reproducible-build pin printed by `nsl autotune --freeze` |
| `--autotune-clean` |  |  | Delete the autotune cache directory and exit |
| `--fusion-report` |  |  | Show fusion optimization report on stderr |
| `--vram-budget` | `<VRAM_BUDGET>` |  | M36: VRAM budget (e.g., "8GB", "512MB") — fail if plan exceeds |
| `--memory-report` |  |  | M36: Print memory plan report |
| `--linear-types` |  |  | M38a: Enable linear types ownership checking |
| `--target` | `<TARGET>` | `cuda` | M47: GPU target backend (cuda, rocm, metal, webgpu) |
| `--disable-fusion` |  |  | Disable all fusion optimizations (for differential testing) |
| `--tape-ad` |  |  | Force tape-based AD (disable source-to-source AD) |
| `--source-ad` |  |  | Use compile-time source-to-source AD instead of runtime tape AD |
| `--pretrain-optimized` |  |  | Pretraining bundle: expands to `--source-ad --wggo greedy --csha auto --fuse-rmsnorm-backward --fuse-wgrad-accum` so one invocation runs WGGO planning, applies CSHA + FASE per the plan, and takes both backward fusions. Explicit flags win (only unset values are filled); conflicts with `--tape-ad` (the planning stack lives in the source-AD path). The two fusions change the ARITHMETIC, not just the schedule — that is why they are opt-in elsewhere. Measured on Coder-50M (batch 4, seq 1024): +14% tok/s, max relative loss divergence 1.2e-5 over 24 steps with an identical trend — a number produced by `--fuse-rmsnorm-backward` and the rest of the bundle, NOT by `--fuse-wgrad-accum`, which needs `grad_accumulation >= 2` in the train block and fused nothing in that run (codegen says so per train block: `[wgrad-fusion] declined: ...`). `--training-reference` strips them back out for a reference run, and `--fuse-wgrad-accum` is skipped (with a note) when `--grad-integrity`, `--optim-state-offload` or `--layerwise-accum` is set, since it refuses those. |
| `--debug-training` |  |  | Debug training: disable fusion + FBIP, emit gradient checksums |
| `--grad-integrity` |  |  | P0.3: gradient-integrity gate — assert every trainable parameter gets a finite, mostly-nonzero gradient each step; prints the [grad-integrity] snapshot at exit. Observes only (no opt changes) |
| `--training-reference` |  |  | P1.7: reference-training mode — disable every training optimization (CCR, CSLA, weight-stream, offload, WGGO, CPDT/precision, CSHA, @fuse, FBIP, fused FASE step, fused-CE) for an independent baseline |
| `--nan-analysis` |  |  | M45: Run compile-time NaN/Inf risk analysis before codegen |
| `--distribute` | `<DISTRIBUTE>` |  | M43: 3D parallelism config (e.g., "dp=2, tp=4, pp=4") |
| `--zero-stage` | `<ZERO_STAGE>` |  | M43: ZeRO optimizer sharding stage (1, 2, or 3) |
| `--zero-elementwise` |  |  | Item 11: elementwise 1/ws parameter sharding under --zero-stage 3. Eligible streamed params (static numel divisible by --devices) live as per-rank slices: all_gather materializes the layer window, gradients reduce_scatter, and every rank runs the fused AdamW step on its own slice. Ineligible params stay tensor-granular. AdamW/Adam only (Muon needs whole matrices) |
| `--deterministic` |  |  | M46: Deterministic mode. Compile-time non-determinism detection AND runtime routing to bit-reproducible kernel variants: the no-atomics embedding backward, the non-coalesced `sum_dim` route, and the flash-attention backward (whose phase-2 kernel accumulates dQ with a cross-CTA float atomicAdd, so its rounding follows scheduling order). The flash backward has no deterministic GPU variant yet, so this routes it to the CPU reference — correct, and roughly 36x the per-step cost at Coder-50M. `--deterministic` already makes that trade elsewhere (its deterministic sum/mean kernels are sequential single-thread PTX); budget for it before using this on a long run. |
| `--matmul-mode` | `<MODE>` | `tf32` | Matmul arithmetic for high-intensity GEMMs: tf32 (default), bf16, f32. Replaces NSL_MATMUL_BF16. Unlike the environment variable this reaches the EXECUTION FINGERPRINT, so a checkpoint records which arithmetic produced it and a resume refuses a silent switch. The fingerprint's `dtype` key does NOT carry this: it is the model dtype and reads `bf16` whatever the GEMMs actually do. |
| `--bf16-rounding` | `<MODE>` | `rne` | Rounding for the bf16 operand cast: rne (default) or sr. Replaces NSL_MATMUL_BF16_ROUND. SR re-dithers per launch, which blocks CUDA graph capture and is incompatible with --bf16-cast-cache |
| `--bf16-min-ratio` | `<RATIO>` | `512` | Minimum arithmetic intensity mnk/(a+b elements) for a GEMM to take the bf16 path. Replaces NSL_MATMUL_BF16_MIN_RATIO. ARITHMETIC: it decides WHICH matmuls are reduced precision |
| `--bf16-cast-cache` |  |  | Cache the weight operand's bf16 cast across GEMMs. Replaces NSL_MATMUL_BF16_CAST_CACHE. Bit-preserving, so a resume only warns -- but it costs ~2 GiB of pinned VRAM at 1B, so enabling it on a resume can OOM a run that previously fit |
| `--bf16-lt` |  |  | Issue bf16-storage GEMMs through cuBLASLt heuristics rather than GemmEx. Replaces NSL_MATMUL_BF16_LT. Changes kernel and reduction order, so it is arithmetic-class for resume |
| `--bf16-lt-workspace-mib` | `<MIB>` | `64` | Workspace cap in MiB for the cuBLASLt heuristic (clamped to 4096). Replaces NSL_MATMUL_BF16_LT_WORKSPACE_MIB. ARITHMETIC: the cap FILTERS candidate kernels, so a smaller value excludes split-k and wide-tile algorithms and changes the reduction order |
| `--no-bf16-lt-tune` |  |  | Disable cuBLASLt timed first-use plan selection. Replaces NSL_MATMUL_BF16_LT_TUNE=0. With tuning ON (the default) the winner depends on live machine state, so plan choice is NOT reproducible across processes |
| `--seed` | `<SEED>` |  | P0 certification: RNG seed for randn/rand/stochastic ops (see `run --seed`) |
| `--dead-weight-threshold` | `<DEAD_WEIGHT_THRESHOLD>` | `0.000001` | M52: Dead weight threshold (default: 1e-6). Weights with \|value\| &lt; threshold are zeroed |
| `--sparse-threshold` | `<SPARSE_THRESHOLD>` | `0.5` | M52: Sparsity threshold (default: 0.5). Matrices with near-zero fraction &gt;= threshold get sparse annotation |
| `--no-constant-fold` |  |  | M52: Disable constant folding of weight expressions |
| `--no-dead-weight` |  |  | M52: Disable dead weight elimination |
| `--no-sparse-codegen` |  |  | M52: Disable sparsity-aware codegen annotations |
| `--shared-lib` |  |  | M62: Build as shared library (.so/.dylib/.dll) with stable C API |
| `--unikernel` |  |  | M54: Build as a bare-metal unikernel image |
| `--listen` | `<LISTEN>` | `0.0.0.0:8080` | M54: Unikernel listen address (default: "0.0.0.0:8080") |
| `--memory` | `<MEMORY>` |  | M54: Unikernel total memory (e.g., "16G", "512M"). 0 or omitted = auto-detect at boot |
| `--wcet` |  |  | M53: Enable WCET analysis for @real_time functions |
| `--wcet-cert` | `<WCET_CERT>` |  | M53: Write WCET certificate JSON to file |
| `--cpu` | `<CPU>` |  | M53: CPU target for WCET analysis (e.g., "cortex-a78") |
| `--do178c-report` | `<DO178C_REPORT>` |  | M53: Write DO-178C compliance report to file (FPGA only) |
| `--wcet-target` | `<WCET_TARGET>` | `gpu` | M53: WCET target: "gpu" (statistical advisory), "fpga" (certified DO-178C), "groq" (blocked) |
| `--fpga-device` | `<FPGA_DEVICE>` |  | M53: FPGA device for certified WCET (e.g., "xcvu440", "xczu9eg", "ve2302") |
| `--zk-circuit` |  |  | M55: Compile @zk_proof functions to ZK inference circuits |
| `--zk-backend` | `<ZK_BACKEND>` | `folding` | M55: ZK proving backend. Only "folding" (default) is implemented; "halo2" (deprecated, circuit lowering removed) and "plonky3" (prover not yet wired into compilation) are refused at build time |
| `--zk-field` | `<ZK_FIELD>` | `m31` | M55: ZK field: m31 (default, ~10x faster) or bn254 (EVM-compatible) |
| `--zk-solidity` |  |  | M55: Emit a Solidity verifier contract alongside the ZK circuit |
| `--zk-weights` | `<ZK_WEIGHTS>` |  | M55: Path to .safetensors weights file used as ZK witness |
| `--wrga-report` | `<PATH>` (optional) |  | WRGA Milestone B.1: Emit the WRGA compilation report. With no value, prints to stdout.  With a path, writes to that file |
| `--wrga-fold-allocations` |  |  | WRGA Milestone B.2 Task 3: fold WRGA memory hints into real allocations.  Default off — observational mode ships per B.1 |
| `--wggo` | `<MODE>` (optional) |  | WGGO: global-optimization mode ("full", "greedy", or "off"). Passing `--wggo` without a value enables full mode |
| `--wggo-report` |  |  | WGGO: print the global-optimization report to stderr |
| `--wggo-moment-precision` |  |  | WGGO: lower the plan's optimizer-moment precision decisions (optim_m/v_bits) to real m/v storage dtypes. Opt-in: changes training numerics. Works on CPU and GPU (the GPU cast envelope uses the CFTP-v7 PTX cast kernels) |
| `--wggo-weights` | `<PATH>` |  | WGGO Stage 3: path to a `.nslweights` sidecar for real weight-based importance scoring.  Without this flag the analyzer falls back to uniform head scores |
| `--wggo-importance` | `<WGGO_IMPORTANCE>` | `auto` | WGGO head-importance scoring mode. - `auto` (default): gradient scoring when calibration sidecar present, else magnitude. - `magnitude`: force magnitude scoring even with calibration data. - `grad`: require gradient scoring; errors if no calibration sidecar present [possible values: `auto`, `magnitude`, `grad`] |
| `--wggo-prune-fraction` | `<F>` |  | WGGO Stage 3: fraction of heads the default `min_retained_importance` threshold allows to be pruned. Clamped to [0.0, 0.9]; default 0.25 |
| `--wggo-memory-budget` | `<MIB>` |  | WGGO: per-device resident training-memory budget in MiB. When set, the planner enforces the cap at both optimization levels (inter-layer DP + per-layer ILP) and, under budget pressure, organically lowers Adam-moment precision to fit — implies --wggo-moment-precision (fp16 moments via the #369 GPU cast envelope are the shipped mechanism). Rejected if 0 or unparseable. A plan that cannot fit even with 16-bit moments is a hard compile error |
| `--optim-state-offload` |  |  | Optimizer-state offload (single-GPU ZeRO-Offload analog): keep the Adam/momentum state (m/v) HOST-resident (pinned when a GPU is live) and stage it to the device for each optimizer step, freeing 1-2x parameter bytes of VRAM. The update math still runs on the GPU in f32, so FASE == AdamW exactness is preserved; the copy-back overlaps the next parameter's update on a dedicated transfer stream. COMPOSES with reduced-precision moments (--wggo-moment-precision / --wggo-memory-budget): host state is then stored at the planned dtype and staged through an on-device quant/dequant envelope, halving the PCIe staging traffic. Env kill-switches: NSL_OFFLOAD_SYNC=1 (synchronous copy-back), NSL_OFFLOAD_PAGEABLE=1 (pageable host buffers). Not supported on pipelined train blocks |
| `--checkpoint-blocks` |  |  | CCR (docs/research/CCR.pdf) block-granular activation checkpointing on the source-AD path: each transformer block's interior activations are freed after the forward and recomputed just before that block's backward, so activation residency drops from O(layers x interiors) to O(layers x boundaries + one block's interiors). Bit-exact — the recompute replays the same kernels in the same order on the same inputs (dropout results are never replayed; they stay saved). No-op with a stderr note on models without `blocks.N`-style structure |
| `--checkpoint-selective` |  |  | With --checkpoint-blocks: selective policy (never recompute matmul-class ops; replay only cheap norms/RoPE/elementwise). Less memory reduction at near-zero recompute cost |
| `--checkpoint-budget-mib` | `<CHECKPOINT_BUDGET_MIB>` |  | With --checkpoint-blocks: allowed SAVED-interior activation budget in MiB. The CCR knapsack flips the most expensive-to-recompute tensors back to SAVE within the budget. Under FASE Deferred the freed-gradient-buffer credit (C-01) is added automatically when parameter sizes are statically known |
| `--checkpoint-stride` | `<CHECKPOINT_STRIDE>` | `1` | With --checkpoint-blocks: periodic checkpointing (Item 8). `N` (an integer &gt;= 1) coalesces every N transformer-block anchors into one CCR super-segment — saving only every N-th block boundary and recomputing the N-block span, for a ~N x smaller saved-boundary surface (the activation surface CSLA buffers across the accumulation window). `auto` searches strides against --checkpoint-budget-mib and picks the smallest projected activation peak that fits. `1` (default) = classic per-block. Bit-exact at any stride. `N` coalesces every N block anchors; `auto` searches uniform strides against the projected activation peak; `dp` (P5 item 21) runs the non-uniform partition DP — minimum GpuSpec-calibrated recompute time subject to --checkpoint-budget-mib, with the CSLA window applied, fusion-aware launch costs and a conservative prefetch-overlap credit. Any choice is bit-exact |
| `--fuse-rmsnorm-backward` |  |  | Item 9 + P5 item 20: lower the source-AD RMSNorm input gradient AND the gamma gradient to fused kernels (native GPU / CPU) instead of the ~11-op / 7-op decompositions — fewer launches, no [rows, cols] temporaries, less HBM traffic. Requires source-AD (`--source-ad`, or `--pretrain-optimized`, which enables this too). Matches the decomposition to an f32 tolerance (opt-in speedup); the fused paths are bit-deterministic run-to-run |
| `--fuse-wgrad-accum` |  |  | Item 7: collapse the source-AD weight-gradient chain (transpose -&gt; batched matmul -&gt; reduce) AND the FASE Deferred accumulate into ONE cuBLAS call, writing straight into m_partial. Removes the [B, d, o] raw-gradient temporary — B times the size of the parameter — and the reduce's full read-back over it. Requires source-AD (`--source-ad`, or `--pretrain-optimized`, which enables this too) and a FASE-Deferred plan. A Deferred plan needs `grad_accumulation >= 2` in the train block. Every block that cannot reach it is named on stderr (`[wgrad-fusion] declined: train block #N — ...`); the flag is REFUSED only when no block in the whole program could fuse, so a program that accumulates in one block and not another still builds. NOT bit-exact: products are summed in cuBLAS's order rather than rounding each per-batch partial first. Measured against an f64 reference it is closer to the true gradient than the chain it replaces in 2 of 3 shapes, but validate loss parity at the repo's 3-4 dp standard before relying on it. Refuses --grad-integrity (which must read the raw gradient this never materializes), --optim-state-offload (host-resident m_partial the device GEMM cannot write), and --layerwise-accum (pre-sliced tapes defeat the fusion's single-reader proof). |
| `--checkpoint-compress` | `<CHECKPOINT_COMPRESS>` |  | With --checkpoint-selective: compress the saved matmul-class activations to half precision between forward and backward (fp16 or bf16). Not bit-exact — backward reads rounded activations; validate loss parity at the 3-4 dp standard [possible values: `fp16`, `bf16`] |
| `--layerwise-accum` |  |  | CSLA Stage-2 (docs/research/CSLA paper §7): window-buffered training schedule. The N micro-batches of a FASE-Deferred accumulation window run their forwards first (buffering only the adjoint-read batch-dependent tensors), then the backward replays the whole window. Bit-exact with the interleaved baseline. Requires --checkpoint-blocks + --source-ad and a FASE-Deferred plan (AdamW/Adam, grad_accumulation &gt;= 2); refuses grad_clip / --optim-state-offload / mode tables loudly |
| `--weight-stream` |  |  | With --layerwise-accum: layer WEIGHT streaming (D2b). Layer- grouped params live in pinned host mirrors and hold a device buffer ONLY inside their brackets: the forward uploads each layer right before its CCR segment and evicts it after its last primal read; the window backward re-uploads per replay range and writes back after that layer's update. Teardown restores residency for model_save/eval. Byte-preserving / bit-exact |
| `--param-dtype` | `<DTYPE>` | `f32` | P4 item 17: authoritative parameter storage dtype for training. `bf16-sr` stores every streamed parameter as a device-resident BF16 buffer (no FP32 master copy) and applies the fused AdamW update with compiler-owned counter-based stochastic rounding (deterministic in --seed, the step, and the parameter/element index). Requires --weight-stream + the fused AdamW step; refuses Muon, ZeRO, offload, and reduced-precision-moment compositions [possible values: `f32`, `bf16-sr`] |
| `--transient-arena` |  |  | Item 5 (Milestone C p2 Stage-2B): place admitted backward temporaries at fixed transient-arena offsets instead of letting the caching allocator choose an address per step. Admission is narrow on purpose — statically sized, non-escaping, non-aliasing backward temporaries produced by an op proven to make exactly one device allocation. Saved tensors, views, external workspaces and optimizer-visible values are excluded by construction. `[arena] placement:` reports what was admitted and, more usefully, what was refused and to which rule. The payoff is address stability for CUDA-graph capture, not bytes. EXPERIMENTAL until `scripts/arena-parity.sh` is green: a liveness error here is silent memory corruption, which is why placement is staged behind the analysis rather than shipped with it. |
| `--fuse-lm-head` | `<MODE>` |  | Item 4: authorize the compiler to install a fused LM head that no `@fused_lm_ce` decorator asked for. `auto` recognises the `matmul(x, transpose(W)) [+ bias] -> cross_entropy` chain, reads `(vocab, hidden)` off the weight's declared dims and the row count off the DataLoader, and falls back to the composite path (saying why, on stderr) when any of that cannot be proven. `require` makes the fallback a compile error. `--pretrain-optimized` selects `auto`. An explicit `@fused_lm_ce` on a train block always wins, including `enabled = false`, which is an opt-out this must not overturn. [possible values: `off`, `auto`, `require`] |
| `--muon-state-dtype` | `<DTYPE>` | `f32` | P4 item 18: Muon optimizer-state storage ladder. `bf16` stores the first-moment (momentum) buffers in BF16 with an FP32 working buffer per update and a counter-based stochastically-rounded store (v stays f32). `int8-blockwise` and `int4-structural` are later ladder rungs — currently refused [possible values: `f32`, `bf16`, `int8-blockwise`, `int4-structural`] |
| `--muon-batch-ns` |  |  | Muon perf campaign: shape-grouped batched Newton-Schulz for all Muon-routed rank-2 hidden matrices — one batched-GEMM pipeline per optimizer step (tensor-core TF32 strided-batched GEMMs over persistent workspaces) instead of ~15 GEMM launches per matrix. GPU-only; tolerance-equivalent to the sequential primitive (the AdamW-routed arm stays bit-for-bit stdlib). Refuses --layerwise-accum, --optim-state-offload and --zero-stage |
| `--muon-resident-momentum` |  |  | Muon perf campaign: with --optim-state-offload, keep Muon-routed params' momentum DEVICE-resident (skips its per-step PCIe stage-in/writeback; AdamW state for embeddings/head/vectors stays offloaded). Removes the mixed recipe's only per-step optimizer-state round trip at the cost of one f32 momentum surface in VRAM |
| `--cuda-graphs` |  |  | P5 item 19: opportunistic per-region CUDA graph capture/replay. Each source-AD lowering (forward slice, backward layer range, recompute segment) records its launch sequence, captures it as a CUDA graph once stable across steps, and replays it with per-launch verification + eager self-repair on any divergence. Requires --source-ad; refuses ZeRO, @pipeline, --cuda-sync and the kernel profiler. Bit-exact (same kernels, same order, same stream). NSL_CUDA_GRAPHS=0 disables at runtime; NSL_CUDA_GRAPH_LOG=1 traces capture decisions |
| `--stream-arena` |  |  | Item 10 (requires --weight-stream): batch each layer's per-param transfers into ONE contiguous host&lt;-&gt;device transfer through a stable, reused device staging arena. Fewer CUDA calls, one large PCIe transaction per layer, stable device addresses, less fragmentation. Bit-exact with the per-param path |
| `--stream-prefetch` |  |  | Item 11 (requires --stream-arena): double-buffer the backward weight stream — prefetch each layer's pack (async HtoD) while the previous layer computes, guarded by a per-pack CUDA event. WGGO activates the overlap only where compute hides the transfer. Bit-exact with the synchronous arena path |
| `--stream-async-writeback` |  |  | Item 11 writeback half (requires --stream-arena): issue each layer pack's post-update DtoH on the transfer stream instead of blocking — the next layer's compute overlaps the writeback; the mirror scatter lands at the runtime's drain points. Bit-exact |
| `--devices` | `<DEVICES>` | `1` | Number of devices in the target cluster (compile-time `world_size`).  Drives WGGO's ZeRO sharding budget and the tensor-parallel `world_size` baked into the artifact.  Unlike the `run` command's `--devices`, this does not spawn processes — it only informs the compiler's global-optimization plan |
| `--csha` | `<MODE>` (optional) |  | CSHA: attention-fusion mode ("auto", "boundary", "pipeline", "block", or "off").  Passing `--csha` without a value enables auto mode |
| `--csha-report` |  |  | CSHA: print the attention-fusion report to stderr |
| `--cpdt` | `<MODE>` (optional) |  | CPDT: planner mode ("full", "zero_only", or "off"). Passing `--cpdt` without a value enables full mode |
| `--cpdt-num-gpus` | `<N>` |  | CPDT: number of GPUs in the target cluster. Required when `--cpdt` is set |
| `--cpdt-intra-bw` | `<BPS>` | `900000000000` | CPDT: intra-node bandwidth in bytes/sec (default 9e11 = 900 GB/s) |
| `--cpdt-inter-bw` | `<BPS>` | `100000000000` | CPDT: inter-node bandwidth in bytes/sec (default 1e11 = 100 GB/s) |
| `--cpdt-report` |  |  | CPDT: emit the full plan to stdout. Implies `--cpdt` (full mode) |
| `--cfie` | `<MODE>` (optional) |  | CFIE: compiler-fused inference-engine mode ("full", "sampling", or "off").  Passing `--cfie` without a value enables full mode.  Overrides the serve block's `@cfie` decorator; serve-block CFIE keys (kv_layout, sampling:, ...) activate CFIE even without this flag |
| `--cfie-report` | `<PATH>` |  | CFIE: also write the inference build report to this file. The report always prints to stderr when CFIE is active |
| `--calibration-data` | `<PATH>` |  | Path to calibration dataset (.bin or .safetensors).  When omitted, calibration is skipped entirely |
| `--calibrate` | `<MODE>` | `required` | Calibration failure policy.  Default `required` aborts the build on infrastructure errors; `best-effort` warns and falls back.  Degenerate-data errors are always fatal |
| `--calibration-samples` | `<N>` | `512` | Number of calibration samples to consume (default 512). Truncated to the dataset size with a warning when smaller |
| `--calibration-batch-size` | `<N>` | `8` | Calibration batch size: samples per harness forward pass (default 8; must be &gt; 0) |
| `--calibration-timeout` | `<SECONDS>` | `600` | Wall-clock limit for the calibration run, in seconds (default 600; must be &gt; 0) |
| `--cep-prune` |  |  | CEP: run compilation-verified pruning (requires --weights) |
| `--cep-joint` |  |  | CEP Mode 3 (paper §2.2): run joint prune-search (heads + FFN + layer drops). Requires --weights. Mutually exclusive with --cep-prune |
| `--cep-target` | `<CEP_TARGET>` |  | CEP: target GPU for analysis (e.g. H100-SXM) |
| `--cep-sparsity` | `<CEP_SPARSITY>` |  | CEP: override the decorator's prune sparsity |
| `--cep-out` | `<CEP_OUT>` |  | CEP: write the delta JSON here (default: &lt;model&gt;.cep.json) |
| `--cep-emit-weights` | `<CEP_EMIT_WEIGHTS>` |  | CEP: also emit the pruned (sliced) weights to this .safetensors path |
| `--cep-emit-source` | `<CEP_EMIT_SOURCE>` |  | CEP SP2: also emit the rewritten NSL source with pruned dims to this path |
| `--activation-report` |  |  | Milestone A: print the activation-contract report — one line per requested optimization surface (flag or decorator) with what became of it (applied / declined / witnessed / UNSATISFIED) |
| `--allow-inert-requests` |  |  | Milestone A escape hatch: demote unsatisfied activation contracts (a requested feature whose owner recorded no disposition) from a hard error to a warning. For migration only — an inert request is a correctness bug in the invocation or the compiler |
| `--allow-unknown-decorators` |  |  | Milestone A escape hatch: demote UNKNOWN-decorator-name errors to warnings (one release, for migration). Documented-but-unimplemented decorators (e.g. @tie_weights) stay hard errors — accepting those would resurrect the silence this flag's namespace close exists to end |

## `nsl test`

Run @test functions in an NSL file

```text
nsl test [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl test file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--filter` | `<FILTER>` |  | Filter tests by name substring |

## `nsl export`

Export model to ONNX or checkpoint to safetensors

```text
nsl export [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Input file (.nsl for ONNX export, .nslm for safetensors conversion) |

| Flag | Value | Default | Description |
|---|---|---|---|
| `-o`, `--output` | `<OUTPUT>` |  | Output file path |
| `--format` | `<FORMAT>` |  | Output format (onnx, safetensors) — inferred from extension if omitted |

## `nsl convert`

Convert between checkpoint formats (.nslm ↔ .safetensors)

```text
nsl convert <INPUT> <OUTPUT>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<INPUT>` | yes |  | Input file (.nslm or .safetensors) |
| `<OUTPUT>` | yes |  | Output file (.safetensors or .nslm) — extension determines direction |

## `nsl init`

Initialize a new NeuralScript project

```text
nsl init <NAME>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<NAME>` | yes |  | Project name (creates directory) |

## `nsl fmt`

Format NeuralScript source files

```text
nsl fmt [OPTIONS] [FILES]...
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILES>` | no |  | Files to format |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--check` |  |  | Check mode: exit non-zero if changes needed |

## `nsl debug`

Read and analyze a tensor operation trace (.nsltrace binary)

```text
nsl debug [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsltrace binary file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--find-nan` |  |  | Find first NaN/Inf operation |
| `--diff` | `<DIFF>` |  | Compare with another trace file |
| `--export-chrome` | `<EXPORT_CHROME>` |  | Export to Chrome tracing JSON file |

## `nsl zk`

M55: ZK inference circuit operations (stats, prove, verify)

```text
nsl zk <COMMAND>
```

Subcommands:

- [`nsl zk stats`](#nsl-zk-stats) — Show circuit statistics for a compiled .zkir file
- [`nsl zk prove`](#nsl-zk-prove) — Generate a ZK proof from a compiled circuit
- [`nsl zk verify`](#nsl-zk-verify) — Verify a ZK proof against public inputs

## `nsl zk stats`

Show circuit statistics for a compiled .zkir file

```text
nsl zk stats <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .zkir file |

## `nsl zk prove`

Generate a ZK proof from a compiled circuit

```text
nsl zk prove [OPTIONS] --pk <PK> --input <INPUT> <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .zkir file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--pk` | `<PK>` |  | Path to the proving key file [required] |
| `--input` | `<INPUT>` |  | Path to JSON file containing circuit inputs [required] |
| `-o`, `--output` | `<OUTPUT>` |  | Output path for the generated proof (default: &lt;file&gt;.proof) |

## `nsl zk verify`

Verify a ZK proof against public inputs

```text
nsl zk verify --proof <PROOF> --public <PUBLIC> <VK>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<VK>` | yes |  | Path to the verification key file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--proof` | `<PROOF>` |  | Path to the proof file to verify [required] |
| `--public` | `<PUBLIC>` |  | Path to JSON file containing public inputs [required] |

## `nsl profile`

Predictive performance profile for a target GPU

```text
nsl profile [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--target` | `<TARGET>` | `h100` | Target GPU (e.g., "h100", "a100", "rtx-4090") |
| `--dtype` | `<DTYPE>` | `bf16` | Dtype to cost (e.g., "bf16", "fp16", "fp8") |
| `--batch` | `<BATCH>` | `1` | Concrete batch size to resolve shape symbols |
| `--seq` | `<SEQ>` | `2048` | Concrete sequence length to resolve shape symbols |
| `--dim` | `<DIM>` (repeatable) |  | Extra shape-env bindings as "name=N" (repeatable) |
| `--no-fusion` |  |  | Disable fusion plan in the report |
| `--memory` |  |  | Include memory timeline in the report (Task 5) |
| `--entry` | `<ENTRY>` | `auto` | Entry point: "auto", "train", or "fn:&lt;name&gt;" |
| `--json` |  |  | Emit machine-readable JSON instead of a formatted table |
| `--explain-wggo` |  |  | Run WGGO Full mode and append/attach the decision explanation |
| `--html` | `<HTML>` |  | Write a self-contained HTML report (inline-SVG roofline plot, per-op table, memory timeline) to this path |

## `nsl autotune`

Measure @autotune kernel variants on this GPU and cache the winners (roadmap item 10 — the measured path; compiles themselves never benchmark). A Measured cache record short-circuits the cost model at every subsequent compile of the same kernel on the same device

```text
nsl autotune [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file containing @autotune kernels |

| Flag | Value | Default | Description |
|---|---|---|---|
| `--elements` | `<ELEMENTS>` | `1048576` | Representative element count for synthesized tensor arguments (matches the cost model's own workload assumption by default) |
| `--freeze` | `<FREEZE>` |  | Also write a frozen tuning database of this run's measured records; prints the sha256 to pin with --autotune-db-sha256 |

## `nsl tokenize`

Train a BPE tokenizer from source files

```text
nsl tokenize [OPTIONS]
```

| Flag | Value | Default | Description |
|---|---|---|---|
| `--dirs` | `<DIRS>` (repeatable) |  | Directories to scan for source files (default: stdlib/ examples/ tests/) |
| `-o`, `--output` | `<OUTPUT>` | `tokenizer.json` | Output path for the tokenizer JSON file |
| `--vocab-size` | `<VOCAB_SIZE>` | `49152` | Vocabulary size (default: 49152 — matches coder50m config) |
| `--min-freq` | `<MIN_FREQ>` | `2` | Minimum token frequency (default: 2) |
| `--ext` | `<EXT>` | `nsl` | File extensions to include (default: .nsl) |
| `--transition` | `<TRANSITION>` | `40960` | Vocabulary size at which the token boundary widens from words to whole lines, letting merges cross word boundaries (SuperBPE-style). Set to --vocab-size or above for ordinary word-bounded BPE. The default spends 5/6 of the vocabulary on ordinary subwords and the last sixth on cross-boundary units, which measured 29.7% fewer tokens for the same corpus while keeping the in-domain/out-of-domain compression gap at 1.24x (full relaxation from the start gives 1.44x). |
| `--max-token-bytes` | `<MAX_TOKEN_BYTES>` | `0` | Longest token surface permitted, in bytes; 0 disables the cap |

## `nsl fpga-compile`

M57: Compile an NSL file to synthesizable Verilog for FPGA targets.

NOTE: --target fpga is not yet end-to-end functional. PRs 1-4 shipped the HIR + KIR-&gt;HIR + HIR-&gt;Verilog + Yosys-gate + fixture infrastructure (M57 milestone).  AST -&gt; structured KIR dispatch, HIR port/wire generation, and CLI dispatch wiring are deferred to M57.1 (v1 closure follow-on).

```text
nsl fpga-compile [OPTIONS] <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to the .nsl file to compile |

| Flag | Value | Default | Description |
|---|---|---|---|
| `-o`, `--output-dir` | `<OUTPUT_DIR>` |  | Output directory for the emitted .v file |
| `--fixture` | `<FIXTURE>` |  | Path to the weight fixture binary (.bin) for the v1 MLP. Defaults to &lt;input_dir&gt;/&lt;source_basename&gt;_weights.bin (sidecar convention). See spec §6.1 for the sidecar lookup rules |
| `--test-taps` |  |  | Emit test-tap ports on all intermediate signals (Layer 2 + Layer 3 gates). Only valid with --target fpga |
| `--seq` |  |  | M57.2: emit the clocked sequential FSM instead of the combinational netlist. Prints `total_cycles=<N>` to stdout after writing the .v file |

## `nsl ptx-metadata`

Report static per-kernel PTX metadata (declared registers, shared memory, target SM) parsed from a synthesized `.ptx` file. Pure text analysis — no GPU or CUDA toolkit required

```text
nsl ptx-metadata <FILE>
```

| Argument | Required | Default | Description |
|---|---|---|---|
| `<FILE>` | yes |  | Path to a `.ptx` file (NSL- or ptxas-generated) |

## `nsl env`

The `NSL_*` environment variables the toolchain reads: list the registry, or show which are set in the current environment

```text
nsl env <COMMAND>
```

Subcommands:

- [`nsl env list`](#nsl-env-list) — Print the registry: every variable with its kind, default, tier and doc
- [`nsl env current`](#nsl-env-current) — Show the NSL_* variables set in this environment and their values, registered ones first; an NSL_* variable that is set but not in the registry is flagged (and makes the exit code 1 with --strict)

## `nsl env list`

Print the registry: every variable with its kind, default, tier and doc

```text
nsl env list [OPTIONS]
```

| Flag | Value | Default | Description |
|---|---|---|---|
| `--tier` | `<TIER>` |  | Only this tier: behavior, perf, safety, platform, diagnostic, test |
| `--markdown` |  |  | Emit the Markdown page that docs/wiki/Environment-Variables.md is generated from (always the whole registry) |
| `--json` |  |  | Emit a JSON array instead of the text table |

## `nsl env current`

Show the NSL_* variables set in this environment and their values, registered ones first; an NSL_* variable that is set but not in the registry is flagged (and makes the exit code 1 with --strict)

```text
nsl env current [OPTIONS]
```

| Flag | Value | Default | Description |
|---|---|---|---|
| `--strict` |  |  | Exit 1 if any set NSL_* variable is not registered |
| `--json` |  |  | Emit JSON |

## `nsl doc`

Generated reference documentation: the CLI and standard-library reference pages

```text
nsl doc <COMMAND>
```

Subcommands:

- [`nsl doc cli`](#nsl-doc-cli) — Emit the Markdown CLI reference that docs/wiki/CLI-Reference.md is generated from: every subcommand with its positionals and flags, in the words of `nsl <command> --help`
- [`nsl doc stdlib`](#nsl-doc-stdlib) — Emit the Markdown standard-library reference that docs/wiki/Stdlib-Reference.md is generated from: every module in `stdlib/` with its functions and models, signatures from the parser and descriptions from `##` doc comments

## `nsl doc cli`

Emit the Markdown CLI reference that docs/wiki/CLI-Reference.md is generated from: every subcommand with its positionals and flags, in the words of `nsl <command> --help`

```text
nsl doc cli
```

## `nsl doc stdlib`

Emit the Markdown standard-library reference that docs/wiki/Stdlib-Reference.md is generated from: every module in `stdlib/` with its functions and models, signatures from the parser and descriptions from `##` doc comments

```text
nsl doc stdlib
```
