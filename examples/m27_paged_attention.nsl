# M27: Paged FlashAttention — decorator validation test
# Tests that @flash_attention decorator parses correctly

@flash_attention(causal=true)
fn paged_attention(Q: Tensor, K: Tensor, V: Tensor) -> Tensor:
    let scale = 1.0 / sqrt(16.0)
    return Q

print("Paged FlashAttention compiled OK")
