# M27: RoPE + GQA variant — decorator validation test

@flash_attention(causal=true)
@gqa(groups=4)
@rope(style="half_split")
fn gqa_attention(Q: Tensor, K: Tensor, V: Tensor) -> Tensor:
    let scale = 1.0 / sqrt(16.0)
    return Q

print("RoPE + GQA compiled OK")
