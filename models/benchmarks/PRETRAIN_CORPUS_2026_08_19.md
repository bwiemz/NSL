# Pretraining corpus — Stack v3 + FineWeb + distillation + NSL sources

Generated by `tools/hfcorpus/report.py` from the corpus manifests; every
figure is read off disk at render time rather than transcribed.

## Sources

| set | dataset | taken |
|---|---|---|
| code | `HuggingFaceCode/stack-v3-train` | 20 of 8,192 shards, strided (full set is 3.54 TB / ~3.6T tokens) |
| web | `HuggingFaceFW/fineweb`, config `sample-10BT` | 14 of 15 parquet for train, the 15th held out whole |
| chat | `r0b0tlab/qwen3.8-max-glm5.2-kimi-k3-distillation`, `canonical` | 52,205 train / 2,872 val / 2,860 test traces |
| nsl | `data/tokcorpus/combined_train.txt` | this repo + 15 unrelated local projects |

Stack shards are sampled with a **stride**, not a prefix: their partitioning
order is undocumented and a prefix could correlate with repo size, language,
or crawl order. The distillation set ships two overlapping shard series in one
directory; the `-of-00006` series is the current export and a naive `train-*`
glob would duplicate 41,682 traces.

## Corpora

| corpus | tokens | on disk | B/tok | source text |
|---|---:|---:|---:|---:|
| `nsl_train.bin` | 11,413,038 | 0.02 GB | 4.2259 | 0.05 GB |
| `pretrain_train.bin` | 22,595,922,912 | 45.19 GB | — | — |
| `sft_train.bin` | 41,020,908 | 0.08 GB | 4.9049 | 0.20 GB |
| `sft_val.bin` | 2,258,310 | 0.00 GB | 4.9098 | 0.01 GB |
| `stack_train.bin` | 7,749,015,576 | 15.50 GB | 3.6993 | 28.67 GB |
| `web_train.bin` | 8,993,175,632 | 17.99 GB | 5.0351 | 45.28 GB |
| `web_val.bin` | 11,942,304 | 0.02 GB | 5.0242 | 0.06 GB |

**16,808,825,768 unique tokens** across the source corpora. The mixture is
built from them and is listed above; it is deliberately excluded from that
sum, since counting both would double-count almost every token.

### `pretrain_train.bin` composition

| part | tokens | share | epochs of that corpus |
|---|---:|---:|---:|
| `base` | 8,993,175,632 | 39.8000% | 1.000 |
| `data/tokens/mix/stack_train.bin` | 13,557,553,819 | 60.0000% | 1.75 |
| `data/tokens/mix/nsl_train.bin` | 45,193,461 | 0.2000% | 3.96 |

Seed `1337`, end-of-document id `49145`.

## Tokenizer: v1 (code-trained) vs v2 (mix-trained)

Same two-stage spec — cl100k→line, `transition=40960`, vocab 49152,
`min_freq=2`. What differs is the training mixture and the reserved
special tokens (v1 reserved none). v2's sample tracks the corpus at
~58% code, so it recovers a pure-code tokenizer's compression on unseen
code while keeping a large gain on web. Bytes/token, higher is better.

| held-out set | v1 | v2 | change |
|---|---:|---:|---:|
| nsl — this repo's held-out sources | 5.371 | 4.149 | -22.8% |
| ood — five unseen code projects | 4.324 | 4.306 | -0.4% |
| web — FineWeb held-out shard | 3.744 | 5.032 | +34.4% |
| sft — distillation validation split | 3.734 | 4.915 | +31.6% |

## Verification

`tools/hfcorpus/verify.py` checks token counts against manifests, that ids
fit the u16 stream, that the end-of-document id is present, and that the
web val set is neither the head nor a suffix of train. Beyond that, three
structural properties were measured directly:

* Documents survive tokenization: `stack_train.bin` carries 424,728
  `<|endoftext|>` ids against 424,728 extracted documents, and 5,282,296
  `<|file_sep|>` markers — 12.4 files per repository.
* Chat structure survives: every `<|im_start|>` in the rendered text maps
  to exactly one id (212,849 of each), and the tool-call counts reproduce
  the dataset card's own mixture table (5,952 / 10.27%).
* The mixture is interleaved, not blocked: `<|file_sep|>` density across
  ten deciles of `pretrain_train.bin` is 388-419 per million tokens, a
  1.08x spread. A run consuming any prefix gets the intended proportions.

* `decode(encode(x))` is exact on all held-out sets: 20,000/20,000 web,
  2,872/2,872 chat, 158/158 code.

## Reserved ids

Not contiguous: surfaces the chat sample taught BPE as ordinary merges
keep the id they were learned at, and `add_special_tokens` reuses it.

| surface | id |
|---|---:|
| `<|endoftext|>` | 49145 |
| `<|im_start|>` | 41256 |
| `<|im_end|>` | 49146 |
| `<|tool_call|>` | 49147 |
| `<|tool_result|>` | 49148 |
| `<|pad|>` | 49149 |
| `<|file_sep|>` | 41190 |


## Addendum (2026-08-24): the committed manifest

The bins above are local-only artifacts. Their committed identity is
`models/datasets/CORPUS_MANIFEST_v2.json` — sha256 + token count per bin, the
sha256 of every committed tokenizer file, the HF **revisions actually
downloaded** (recovered from `huggingface_hub`'s download metadata, not
memory), the on-disk parquet lists, per-set dedup status, and the reserved
surfaces/ids. `tools/hfcorpus/manifest.py build` regenerates it;
`manifest.py check --hash-bins` re-verified all 45 GB byte-for-byte on
2026-08-24 (11/11 PASS).

The reserved-surface list now has ONE copy (`models/tokenizers/
special_tokens.json`); `extract.py` and `train_tokenizer.py` load it, and
`corpus_manifest_gate.rs` refuses an inline list growing back, a divergence
between the manifest / record / tokenizer ids, a committed tokenizer whose
hash drifts from the record, and an HF source without a pinned revision.
