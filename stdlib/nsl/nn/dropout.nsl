## Dropout regularisation.

## Zeroes each element with probability `p` during training and rescales
## the survivors so the expected activation is unchanged.
model Dropout(p: float):
    prob: float = p

    fn forward(self, x: Tensor) -> Tensor:
        return dropout(x, self.prob, is_training())
