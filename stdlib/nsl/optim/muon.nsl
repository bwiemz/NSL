fn muon_step(param: Tensor, grad: Tensor, velocity: Tensor, lr: float, momentum: float, nesterov: bool):
    copy_data(velocity, momentum * velocity + grad)
    if nesterov:
        let update = grad + momentum * velocity
    else:
        let update = velocity
    copy_data(param, param - lr * update)
