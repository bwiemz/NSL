# Test Task 1: nsl_tensor_unsqueeze
# [3,4] unsqueeze(0) -> [1,3,4]
let a = full([3, 4], 2.0)
let b = a.unsqueeze(0)
print(b)

# free-function form
let c = unsqueeze(a, 1)
print(c)

# negative dim: unsqueeze(-1) on [3,4] -> [3,4,1]
let d = a.unsqueeze(-1)
print(d)

print("unsqueeze: PASS")
